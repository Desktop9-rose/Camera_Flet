name: Flet Camera APK Build - Fixed

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 检出代码
      uses: actions/checkout@v4

    - name: ☕ 设置 Java 环境 (Flutter 需要)
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: 🐦 设置 Flutter 环境
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.22.0' # 使用与 Flet 兼容的稳定版 Flutter
        channel: 'stable'

    - name: 🐍 设置 Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: 📦 安装依赖
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: 🏗 构建 APK
      run: |
        # 直接使用 flet 命令，而不是 python -m flet
        flet build apk --verbose

    - name: 📤 上传 APK
      uses: actions/upload-artifact@v4
      with:
        name: flet-camera-apk
        path: build/apk/app-release.apk
        retention-days: 30