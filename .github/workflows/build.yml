name: Flet Camera Final - TOML Config

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 检出代码
      uses: actions/checkout@v4

    - name: ☕ 设置 Java 环境
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: 🐦 设置 Flutter 环境
      uses: subosito/flutter-action@v2
      with:
        # Flet 0.23.2 必须配合 Flutter 3.19.6
        flutter-version: '3.19.6'
        channel: 'stable'

    - name: 🤝 签署 Android 协议
      run: |
        yes | flutter doctor --android-licenses

    - name: 🐍 设置 Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: 📦 安装构建工具
      run: |
        python -m pip install --upgrade pip
        # 安装核心库
        pip install flet==0.23.2
        pip install -r requirements.txt

    - name: 🏗 构建 APK
      run: |
        # 核心修复：移除所有参数，只保留 verbose
        # 配置信息全部由 pyproject.toml 提供
        flet build apk --verbose

    - name: 📤 上传 APK
      uses: actions/upload-artifact@v4
      with:
        name: flet-camera-apk
        path: build/apk/app-release.apk
        retention-days: 30