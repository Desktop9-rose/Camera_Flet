name: Flet Camera APK Build - Fixed Version

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 检出代码
      uses: actions/checkout@v4

    - name: 🐍 设置Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: 📦 安装指定版本Flet
      run: |
        python -m pip install --upgrade pip
        # 安装一个已知稳定的Flet版本
        pip install flet==0.22.0
        pip install pillow

    - name: 🔧 创建flet配置文件
      run: |
        cat > flet.json << EOF
        {
          "name": "FletCamera",
          "description": "Camera Demo App",
          "version": "1.0.0",
          "package": "com.example.fletcamera"
        }
        EOF

    - name: 🛠 修复构建环境
      run: |
        # 清理可能的缓存
        rm -rf .flet
        rm -rf build

    - name: 🏗 使用简化的构建命令
      run: |
        # 使用最基本的构建命令
        python -m flet build apk --verbose

    - name: 📱 查找APK文件
      run: |
        find . -name "*.apk" -type f | head -10
        mkdir -p artifacts
        find . -name "*.apk" -exec cp {} artifacts/ \; 2>/dev/null || true
        ls -la artifacts/

    - name: 📤 上传APK
      uses: actions/upload-artifact@v4
      with:
        name: flet-camera-apk
        path: artifacts/
        retention-days: 30