name: Flet Camera Fix - 0.23.2 / Flutter 3.22

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 检出代码
      uses: actions/checkout@v4

    - name: ☕ 设置 Java 环境
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: 🐦 设置 Flutter 环境
      uses: subosito/flutter-action@v2
      with:
        # 关键修复：升级到 3.22.2 以支持 Flet 0.23.2 的代码
        flutter-version: '3.22.2'
        channel: 'stable'

    - name: 🤝 签署 Android 协议
      run: |
        yes | flutter doctor --android-licenses

    - name: 🐍 设置 Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: 📦 安装构建工具
      run: |
        python -m pip install --upgrade pip
        # 安装 Flet 0.23.2
        pip install flet==0.23.2
        pip install -r requirements.txt

    - name: 🏗 构建 APK
      run: |
        # 0.23.2 支持读取 pyproject.toml
        flet build apk --verbose

    - name: 📤 上传 APK
      uses: actions/upload-artifact@v4
      with:
        name: flet-camera-apk
        path: build/apk/app-release.apk
        retention-days: 30