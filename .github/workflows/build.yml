name: Flet Camera APK Build - Final Fix

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 检出代码
      uses: actions/checkout@v4

    - name: ☕ 设置 Java 环境
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: 🐦 设置 Flutter 环境
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.24.0' # 使用较新的稳定版
        channel: 'stable'

    - name: 🤝 签署 Android 协议
      run: |
        # 这一步至关重要，解决 License 报错
        yes | flutter doctor --android-licenses

    - name: 🐍 设置 Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: 📦 安装依赖
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: 🏗 构建 APK
      run: |
        # 关键修复：
        # 1. --project-name: 强制命名为 flet_cam，防止与 'flet' 包名冲突
        # 2. --org: 指定包名组织，避免默认冲突
        # 3. --product-name: 安装在手机上显示的名字
        flet build apk --verbose \
          --project-name flet_cam \
          --org com.mycamera \
          --product-name "Flet相机" \
          --description "A simple camera app"

    - name: 📤 上传 APK
      uses: actions/upload-artifact@v4
      with:
        name: flet-camera-apk
        path: build/apk/app-release.apk
        retention-days: 30