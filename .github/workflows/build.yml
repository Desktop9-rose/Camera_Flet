name: Flet Camera APK Build - Final Clean

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 检出代码
      uses: actions/checkout@v4

    - name: ☕ 设置 Java 环境
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: 🐦 设置 Flutter 环境
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.22.0' # 严格匹配 Flet 0.24.1
        channel: 'stable'

    - name: 🤝 签署 Android 协议
      run: |
        yes | flutter doctor --android-licenses

    - name: 🐍 设置 Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: 📦 安装依赖
      run: |
        python -m pip install --upgrade pip
        # 安装 requirements.txt (现在只有 flet==0.24.1)
        pip install -r requirements.txt

    - name: 🏗 构建 APK
      run: |
        # 构建命令
        flet build apk --verbose

    - name: 📤 上传 APK
      uses: actions/upload-artifact@v4
      with:
        name: flet-camera-apk
        path: build/apk/app-release.apk
        retention-days: 30